{{> publicNavBar}}
<div class="container">
  <section class="mt-4 border p-3 rounded-2 bg-light">
    <h2 class="text-center">Login</h2>
    <p class="text-center">Enter your account details to access the dashboard and our full suite of blog features.</p>
    <hr />
    <form class="mt-4" id="login-form">
      <div class="mb-3">
        <input
          type="username"
          class="form-control"
          id="username"
          placeholder="Enter username"
        />
      </div>
      <div class="mb-3">
        <input
          type="password"
          class="form-control"
          id="password"
          placeholder="Enter password"
        />
      </div>
      <div id="login-error"></div>
      <div class="mb-3 text-center">
        <button type="submit" class="btn btn-primary">Login</button>
      </div>
      <div class="mb-3 text-center error-text">
        Don't have an account?
        <a href="/signup">Sign up here.</a>
      </div>
    </form>
  </section>
</div>