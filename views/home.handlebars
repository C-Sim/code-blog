{{#if isLoggedIn}}
  {{> privateNavBar}}
{{else}}
  {{> publicNavBar}}
{{/if}}

<h1 class="text-center mt-3">Recent Blogs</h1>

<div id="hp-blog">

{{#each blogs as |blog|}}
  {{> allBlogs}}
{{/each}}

</div>